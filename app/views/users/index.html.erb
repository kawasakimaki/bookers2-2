<!--左側-->
<%= render 'books/left', book: @book, user: current_user %>

<!--右側-->
<h4>Users</h4>

<table class="table">
  <tr>
    <th>image</th>
    <th>name</th>
    <th></th>
  </tr>

  <% @users.each do |user| %>
    <tr>
      <td>画像</td>
      <td><%= user.name %></td>
      <td><%= link_to 'Show', user_path(user.id) %></td>
    </tr>
  <% end %>
</table>
